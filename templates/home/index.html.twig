{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
    <div id="carouselExampleCaptions" class="carousel slide carousel-fade" data-ride="carousel">
        <div class="carousel-inner">
            <div class="carousel-item active">
                <img src="https://localhost:8000/assets/images/home/home.jpg" class="d-block img-fluid home_slider"
                     alt="home"
                     style="filter: brightness(60%)">
                <div class="carousel-caption">
                    <span class="promo-content">
                        <span class="title">Bonjour à tout le monde</span>
                        <span class="decor d-none d-md-block"><i class="icon icon-rosh"></i></span>
                        <span class="note">
                            Notre magasin Saveurs d'Arménie est ravi de vous accueillir et de vous proposer ses services, ses produits frais, ainsi qu’un choix varié de produits traditionnels d'Arménie, de Géorgie, de Pologne, d'Ukraine, de Moldavie, de Russie...
                        </span>
                    </span>
                </div>
            </div>
            <div class="carousel-item">
                <img src="https://localhost:8000/assets/images/subcats/{{ categories[2].subcats[5].picture }}" class="d-block img-fluid home_slider"
                     alt="basturma"
                     style="filter: brightness(40%)">
                <div class="carousel-caption">
                    <span class="promo-content">
                        <a href="{{ path('prodlist2', {id:15}) }}">
                            <span class="title">{{ categories[2].subcats[5].title }}</span>
                        </a>
                        <span class="decor"><i class="icon icon-rosh"></i></span>
                        <span class="note d-none d-md-block">{{ categories[2].subcats[5].text }}</span>
                    </span>
                </div>
            </div>
            <div class="carousel-item">
                <img src="https://localhost:8000/assets/images/subcats/{{ categories[7].subcats[2].picture }}" class="d-block img-fluid home_slider"
                     alt="basturma"
                     style="filter: brightness(40%)">
                <div class="carousel-caption">
                    <span class="promo-content">
                        <a href="{{ path('prodlist2', {id:47}) }}">
                            <span class="title">{{ categories[7].subcats[2].title }}</span>
                        </a>
                        <span class="decor"><i class="icon icon-rosh"></i></span>
                        <span class="note d-none d-md-block">{{ categories[7].subcats[2].text }}</span>
                    </span>
                </div>
            </div>
            <div class="carousel-item">
                <img src="https://localhost:8000/assets/images/subcats/{{ categories[2].subcats[3].picture }}" class="d-block img-fluid home_slider"
                     alt="caviar"
                     style="filter: brightness(40%)">
                <div class="carousel-caption">
                    <span class="promo-content">
                        <a href="{{ path('prodlist2', {id:13}) }}">
                            <span class="title">{{ categories[2].subcats[3].title }}</span>
                        </a>
                        <span class="decor"><i class="icon icon-rosh"></i></span>
                        <span class="note d-none d-md-block">{{ categories[2].subcats[3].text }}</span>
                    </span>
                </div>
            </div>
            <div class="carousel-item">
                <img src="https://localhost:8000/assets/images/subcats/{{ categories[0].subcats[1].picture }}" class="d-block img-fluid home_slider"
                     alt="brandy"
                     style="filter: brightness(40%)">
                <div class="carousel-caption">
                    <span class="promo-content">
                        <a href="{{ path('prodlist2', {id:2}) }}">
                            <span class="title">{{ categories[0].subcats[1].title }}</span>
                        </a>
                        <span class="decor">
                            <i class="icon icon-rosh"></i>
                        </span>
                        <span class="note d-none d-md-block">{{ categories[0].subcats[1].text }}</span>
                    </span>
                </div>
            </div>
            <div class="carousel-item">
                <img src="https://localhost:8000/assets/images/subcats/{{ categories[5].subcats[1].picture }}" class="d-block img-fluid home_slider"
                     alt="khinkali"
                     style="filter: brightness(40%)">
                <div class="carousel-caption">
                    <span class="promo-content">
                        <a href="{{ path('prodlist2', {id:30}) }}">
                            <span class="title">{{ categories[5].subcats[1].title }}</span>
                        </a>
                        <span class="decor">
                            <i class="icon icon-rosh"></i>
                        </span>
                        <span class="note d-none d-md-block">{{ categories[5].subcats[1].text }}</span>
                    </span>
                </div>
            </div>
            <div class="carousel-item">
                <img src="https://localhost:8000/assets/images/subcats/{{ categories[0].subcats[0].picture }}" class="d-block img-fluid home_slider"
                     alt="vin georgien"
                     style="filter: brightness(40%)">
                <div class="carousel-caption">
                    <span class="promo-content">
                        <a href="{{ path('prodlist2', {id:1}) }}">
                            <span class="title">{{ categories[0].subcats[0].title }}</span>
                        </a>
                        <span class="decor">
                            <i class="icon icon-rosh"></i>
                        </span>
                        <span class="note d-none d-md-block">{{ categories[0].subcats[0].text }}</span>
                    </span>
                </div>
            </div>
            <div class="carousel-item">
                <img src="https://localhost:8000/assets/images/subcats/{{ categories[8].subcats[0].picture }}" class="d-block img-fluid home_slider"
                     alt="vin georgien"
                     style="filter: brightness(40%)">
                <div class="carousel-caption">
                    <span class="promo-content">
                        <a href="{{ path('prodlist2', {id:48}) }}">
                            <span class="title">{{ categories[8].subcats[0].title }}</span>
                        </a>
                        <span class="decor">
                            <i class="icon icon-rosh"></i>
                        </span>
                        <span class="note d-none d-md-block">{{ categories[8].subcats[0].text }}</span>
                    </span>
                </div>
            </div>
        </div>
        <a class="carousel-control-prev" href="#carouselExampleCaptions" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleCaptions" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
    <div class="banner-content"></div>
    {#    {{ dump(categories[2].subcats) }} #}
{% endblock %}